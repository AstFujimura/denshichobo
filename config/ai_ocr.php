<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Provider 選択
    |--------------------------------------------------------------------------
    |
    | 現状は gemini。将来 internal に切り替えるだけで投げ先を変更できるようにします。
    |
    */
    // TAMERU（帳簿保存）専用の切替。名刺管理など他機能には影響させない。
    'provider' => env('TAMERU_AI_OCR_PROVIDER', env('AI_OCR_PROVIDER', 'gemini')),

    /*
    |--------------------------------------------------------------------------
    | 社内AIサーバー設定（将来用）
    |--------------------------------------------------------------------------
    */
    'internal' => [
        'base_url' => env('INTERNAL_AI_OCR_BASE_URL', ''),
        'ledger_path' => env('INTERNAL_AI_OCR_LEDGER_PATH', '/ocr/ledger'),
        'token' => env('INTERNAL_AI_OCR_TOKEN', ''),
        'timeout_seconds' => env('INTERNAL_AI_OCR_TIMEOUT', 60),
    ],

    /*
    |--------------------------------------------------------------------------
    | 帳簿保存/変更: OCR 抽出プロンプト
    |--------------------------------------------------------------------------
    |
    | 将来モデルやAPI先を差し替えても、プロンプトはここを基本に固定して運用します。
    |
    */
    'ledger_prompt' => <<<'PROMPT'
あなたは帳簿保存用のOCR抽出器です。
添付ファイル（領収書・請求書・納品書等）から、次の3項目だけを抽出してください。

- 取引日（YYYY/MM/DD 形式。日付が複数ある場合は取引日らしいものを1つ）
- 金額（数値。カンマや通貨記号は除去。税込合計を優先）
- 取引先（文字列。後述の「受領・提出」に応じた企業名・店名を1つ）

出力は必ず JSON のみ（説明文なし）で、次のキーを含めてください:
{"hiduke":"YYYY/MM/DD","kinngaku":12345,"torihikisaki":"..." }
PROMPT,

    // 受領・提出の選択に応じて ledger_prompt に追記される（取引先の意味）
    'ledger_prompt_torihikisaki_jyuryo' => <<<'PROMPT'
【受領・提出】受領 が選択されています。
取引先（torihikisaki）には、書類の「発行元」の企業名・店名を入れてください。
- 発行元: 領収書・請求書を発行した側（請求元、売主、店舗名、発行者名、「発行」「発行日」付近の社名）
- 入れないもの: 自社名（宛名・御中・様の側）、住所・電話のみ、登録番号のみ
PROMPT,

    'ledger_prompt_torihikisaki_teishutsu' => <<<'PROMPT'
【受領・提出】提出 が選択されています。
取引先（torihikisaki）には、書類の「発行先」（宛先）の企業名・店名を入れてください。
- 発行先: 提出先・請求先・宛名側（「御中」「様」、宛名欄、納品先、請求先会社名）
- 入れないもの: 自社名（発行元・差出人側）、住所・電話のみ、登録番号のみ
PROMPT,

    // アップロード上限（KB）。Laravel の validation max は KB 単位。
    'max_file_kb' => 20480,

    // true: laravel.log に詳細ログ、API レスポンスに raw を含める、画面で step を alert
    'trace' => env('TAMERU_AI_OCR_TRACE', false),
];

